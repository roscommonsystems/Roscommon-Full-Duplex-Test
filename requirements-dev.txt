# The test suite itself only needs aiohttp + pytest (the ASR and HF deps are
# stubbed out), but installing the full runtime keeps dev on the same pins as
# the GPU host.
-r requirements.txt
pytest>=8.0
pytest-asyncio>=1.0
# Must stay below 1.1: pytest-aiohttp 1.1+ requires aiohttp>=3.11.0b0, which
# contradicts the aiohttp<3.11 that moshi forces in requirements.txt. Pinning
# it here keeps tests running against the same aiohttp the GPU host uses.
pytest-aiohttp>=1.0,<1.1
