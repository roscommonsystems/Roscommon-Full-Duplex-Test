# Test-only dependencies. Not self-contained -- the suite imports aiohttp, a
# runtime dep, so install both files:
#
#     pip install -r requirements.txt -r requirements-dev.txt
#
# requirements.txt stays test-free on purpose: provision.sh installs it on a
# rented GPU, which is not where you want to find a broken pin. Keep this list
# free of anything version-coupled to the runtime -- see tests/conftest.py for
# why pytest-aiohttp is not here.

pytest>=8.0
pytest-asyncio>=1.0
