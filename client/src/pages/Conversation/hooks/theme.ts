// The UI is fixed to the light theme (see Queue.tsx). This is the shared type
// the audio components take, so a theme can be threaded through if that changes.
export type ThemeType = "light" | "dark";
